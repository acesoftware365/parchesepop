{
  "name": "parchese-pop-firebase-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:database-rules": "firebase emulators:exec --only database --project parchese-pop 'node --test test/firebase_rules/database_rules.test.mjs'",
    "test:firebase-smoke": "firebase emulators:exec --only database --project parchese-pop 'node --test test/firebase_rules/multiclient_smoke.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1"
  }
}
